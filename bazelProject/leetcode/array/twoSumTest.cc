#include "gtest/gtest.h"
#include <vector>

#include "1.twoSum.cc"

using namespace std;

TEST(message_test,content)
{
	std::vector<int> vec = {2,7,4,6,9,3}
    EXPECT_EQ(std::make_pair(0,1),twosum(vec,9);
}